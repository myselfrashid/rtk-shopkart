import React from 'react';
import { useDispatch } from 'react-redux';
import { cartTotal } from '../Features/cartReducer';

const Total = () => {
    const dispatch = useDispatch();
    
    return (
        <div>Total</div>
    )
}

export default Total